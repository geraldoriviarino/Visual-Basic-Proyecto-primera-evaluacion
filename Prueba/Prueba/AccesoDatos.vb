Public Class AccesoDatos

    'Función cuya FUNCIÓN es gestionar la GESTIÓN de ingentes cantidades de permisos. 

    'Para verificar que, en el momento de hacer login, la contraseña introducida por el usuario
    'está asociada en el fichero usuarios.txt al nombre introducido.
    'Devuelve true si el usuario ha introducido un nombre y contraseña correctos, de lo contrario devuelve false.
    Function validarPermiso(ByVal usuario As Usuario) As Boolean

        Return True
    End Function

    'Método encargado de acceder al fichero productos.txt con el fin de
    ' actualizar el precio de un producto determinado.
    'Pendiente de plantear
    Sub modificarPrecio(ByRef producto As Producto)

    End Sub
End Class
